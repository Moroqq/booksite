/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Только WebP. AVIF на сервере с одним ядром кодируется ~5,7 с против ~0,8 с у WebP,
    // а выигрыш в весе почти нулевой — именно из-за него картинки грузились долго.
    formats: ["image/webp"],
    // Сколько обработанная картинка живёт в кеше сервера и браузера.
    // По умолчанию 60 секунд: браузер перепроверял каждую картинку почти при каждом заходе,
    // а сервер заново её кодировал. 30 дней убирают и то, и другое.
    // При замене картинки сохраняйте её под новым именем, иначе месяц будет отдаваться старая.
    minimumCacheTTL: 2592000,
  },
  async headers() {
    return [
      {
        // Вступительная анимация не меняется — пусть браузер держит её у себя
        // и не перезапрашивает при каждом заходе. Если будете менять анимацию,
        // сохраните файлы под новым именем (intro-v2.webm), иначе месяц будет
        // показываться старая версия из кеша.
        source: "/:file(intro.*)",
        headers: [{ key: "Cache-Control", value: "public, max-age=2592000" }],
      },
    ];
  },
};

export default nextConfig;
